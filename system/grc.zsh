# # GRC colorizes nifty unix tools all over the place

## Disabling because of problems with make tasks that start inertactive shells
# if $(which grc &>/dev/null) && $(which brew &>/dev/null)
# then
#   source `brew --prefix`/etc/grc.zsh
# fi
